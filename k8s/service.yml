apiVersion: v1
kind: Service
metadata:
  name: rag-system-service
  namespace: rag-system
spec:
  selector: 
    app: rag-system 
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000  
  type: LoadBalancer